node_modules
.next
.env*.local
.env
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel

# Backup local de imagens originais (geradas pelo optimize-images.mjs)
public/img.original

# IMPORTANTE: /out NÃO está gitignored — é o output do static export que
# a Hostinger puxa via Auto Deploy do Git pra servir como site.
# Roda `npm run build` antes de cada commit que mexer em código/imagens.
